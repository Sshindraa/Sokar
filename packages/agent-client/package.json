{
  "name": "@sokar/agent-client",
  "version": "0.0.1",
  "private": true,
  "description": "Typed SDK + multi-turn LLM runner for the Sokar generic agent REST API.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf .turbo dist"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "typescript": "^5.8.0",
    "vitest": "^4.1.5"
  },
  "dependencies": {}
}
