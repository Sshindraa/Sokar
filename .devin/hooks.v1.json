{
  "UserPromptSubmit": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "echo '{\"hookSpecificOutput\":{\"hookEventName\":\"UserPromptSubmit\",\"additionalContext\":\"[AUTO-ORCHESTRATE] Use the `orchestrate` skill for this prompt. Except for its narrow direct-answer fast path, first analyze, understand, and decompose the request; delegate work to the appropriate workers; run fresh independent read-only reviewers; reconcile findings; perform proportional verification; and report actual results. Only `subagent_general` may write files, while `subagent_explore` performs research and review.\"}}'"
        }
      ]
    }
  ]
}
