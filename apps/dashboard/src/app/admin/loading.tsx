import RouteLoadingState from '@/components/RouteLoadingState';

/** Skeleton de streaming de l'espace opérateur `/admin`. */
export default function AdminLoading() {
  return <RouteLoadingState />;
}
