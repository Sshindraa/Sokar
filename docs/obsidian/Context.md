# Contexte Sokar

## Dernière activité

2026-07-23 14:55 — [voice, hotfix] **Qualité du dernier appel corrigée, prête au déploiement** — L’extraction du nom coupe maintenant les consignes internes du prompt : l’accueil ne peut plus prononcer « L’accueil a déjà été prononcé… ». Lorsque la disponibilité ne retourne aucun créneau vérifié, la réponse propose le gérant ou la prise de message, sans inventer d’alternative. Après une réponse STT ambiguë au nombre de personnes, l’assistant demande explicitement de le préciser. L’allowlist d’enregistrement demeure le double verrou ; `test-resto-1` doit être ajouté à `CALL_RECORDING_TEST_RESTAURANT_IDS` en production. Tests du contrôleur 24/24 et lint API verts ; typecheck global bloqué par un fixture floor-plan existant hors périmètre. Déploiement production en attente.

2026-07-23 14:12 — [voice, echo, barge-in] **Le filtre de l’accueil est remplacé par une détection d’écho générique** — L’appel de test a confirmé que Deepgram entendait « Bonjour » sans parole de l’appelant : retour acoustique de la synthèse TTS. Le code ne bloque plus l’interruption durant l’accueil. Chaque phrase effectivement envoyée à Telnyx est mémorisée seulement pendant sa lecture + 600 ms ; une transcription qui en reproduit exactement une séquence est ignorée, y compris si elle est finalisée. Toute divergence de l’appelant (même après un mot commun) déclenche immédiatement le barge-in normal. Tests de régression écho, écho finalisé et interruption réelle ajoutés ; suite voix complète 276/276, typecheck, lint et diff-check verts. Aucun déploiement production effectué.

2026-07-23 12:58 — [voice, production, ops] **Canary Context V2 activé sur le restaurant de test** — `main` `53a2545` déployé en production avec API, dashboard, Connect et endpoints de santé verts. `VOICE_TTS_CONTEXT_V2_ENABLED=true` est actif uniquement côté API ; ConfigCat renvoie `true` pour `test-resto-1` et `false` pour `control`. L’appel sortant contrôlé est bloqué côté Telnyx : l’application Call Control active n’a aucun outbound voice profile ; aucun profil n’existait sur le compte. Aucun appel ni enregistrement n’a été créé. La suite est soit un appel entrant vers le numéro Sokar de test, soit une validation explicite d’une configuration Telnyx sortante strictement plafonnée.

2026-07-23 12:22 — [voice, canary] **Context V2 Cartesia isolé après le correctif d’interruptions** — La branche `codex/voice-context-v2-canary`, fondée sur `main` fusionné (`e6b4abe`), ajoute le contexte WebSocket Cartesia par réponse LLM, le contrôleur conversationnel et la télémétrie par tour. L’activation exige `VOICE_TTS_CONTEXT_V2_ENABLED=true` et le flag ConfigCat `voice_tts_context_v2` pour le restaurant ciblé ; HTTP `/tts/bytes` reste le chemin par défaut. Suite API complète 1 395/1 395 (6 ignorés), voix 273/273, typecheck, lint et `git diff --check` verts. Aucun déploiement ni activation effectués.

2026-07-23 — [ops, staging] **Récupération staging durcie après health checks en échec** — Le script valide le secret HMAC Service Copilot avant d’arrêter Dashboard/Connect, conserve explicitement le code d’échec et restaure les services dès qu’un health check API, livez, Dashboard ou Connect échoue, avant toute rotation des snapshots. `bash -n` vert. Aucun déploiement.

2026-07-23 — [voice, api, git] **Correctifs voix isolés depuis `origin/main`** — Branche propre limitée à l’accueil « Bonjour, ici … Je vous écoute. », l’enregistrement temporaire soumis au double verrou activation + allowlist restaurant, et l’invalidation par génération des réponses interrompues. `UtteranceStart`, `SpeechResumed` et le barge-in annulent le réseau, les fillers, les callbacks LLM et les trames TTS obsolètes. La suite du dernier `main` passe de 227 à 232 tests voix, tous verts ; typecheck et lint API verts. Les tests canary qui portaient le total local mélangé à 263 sont volontairement exclus. Aucun déploiement.

2026-07-23 — [security, GitHub, production] **Durcissement complémentaire après audit public** — Graphe de dépendances, alertes/correctifs Dependabot (groupés + malware) et CodeQL par défaut activés. La branche `main` impose désormais une pull request, la résolution des conversations et une branche à jour ; Secret Scanning, CodeQL, API, Dashboard, Sokar Connect et Packages sont obligatoires, y compris pour les administrateurs. Suppression et force-push restent interdits. La deploy key VPS en écriture a été remplacée et révoquée : le VPS utilise maintenant une clé dédiée lecture seule, vérifiée par `git ls-remote`. Le correctif E2E floor-plan rend l’assertion du prochain créneau indépendante du libellé de date localisé.

2026-07-23 — [security, git] **Historique public purgé après exposition de secrets** — Les fichiers historiques racine `.env` / `.env.example` et `scripts/deploy.sh` ont été supprimés de toutes les branches GitHub puis force-pushés depuis un miroir isolé, sans toucher le worktree de développement. Les alertes Resend et Callyx/Doppler confirment la révocation de leurs clés ; les anciens secrets de fournisseurs, base/Redis et signatures doivent être renouvelés côté tableaux de bord et environnements. Gitleaks est maintenant lancé sur chaque push et le hook local refuse les fichiers `.env` runtime ainsi que des formats de clés fournisseurs courants. Secret Protection GitHub et Push Protection sont activées : un secret reconnu est bloqué avant la réception du push.

2026-07-23 — [security, production] **Clé SMTP Resend renouvelée en production** — Une nouvelle clé Resend à permission d’envoi a remplacé `SMTP_PASS` dans `/opt/sokar/apps/api/.env`, par canal temporaire sans journalisation de la valeur. `sokar-api` a été rechargé seul et son health check local est vert. Le jeton Callyx/Doppler révoqué n’est plus présent dans `sokar/stg`, et aucun code courant ne dépend de Doppler. Restent à renouveler les éventuelles clés actives des fournisseurs voix/LLM, les mots de passe DB/Redis et les secrets de signature/authentification signalés par l’audit historique.

2026-07-22 11:35 — [api, dashboard, database, ops] **Déploiement production VPS (commit `50576b9`)** — Push sur `main` et exécution de `scripts/deploy-vps.sh --confirm-production`. Inclusion de la qualification des recommandations Copilot après service, rééquilibrage de serveur atomique (`POST /service-copilot/actions/server-rebalance`), vue `/dashboard/copilot/quality`, et migration Prisma `20260722000003_copilot_telemetry`. Vérifications de santé VPS API local/public et Sokar Connect 200 OK. Suite de tests monorepo 100% verte (1327 API tests + 119 Dashboard tests).

2026-07-22 01:17 — [dashboard, service-copilot] **Qualité Copilot séparée de la Salle** — Nouvelle page `/dashboard/copilot/quality`, accessible depuis la carte Copilot du dashboard et jamais depuis le Live service. Elle lit le résumé de télémétrie sur 7/30/90 jours, affiche les recommandations suivies, décisions appliquées (y compris celles ensuite annulées), conflits, signaux sans suite et le dernier résultat par type de suggestion. États chargement, vide et erreur avec relance inclus. La page explicite que cette lecture se fait après le service ; aucun élément visuel n’est ajouté au plan de salle. Tests composant 2/2, typecheck et lint dashboard verts. Aucun déploiement de cette vue.

2026-07-22 01:06 — [database, api, dashboard, service-copilot] **Télémétrie de décision discrète / shadow mode** — Ajout additif des occurrences et événements Copilot, avec token HMAC serveur par recommandation, cloisonnement restaurant, clé d’idempotence et hachage de l’acteur. Le Live service n’affiche aucun nouveau contrôle : il enregistre silencieusement `viewed` / `opened`; le flux transactionnel de récupération de retard écrit côté serveur `applied`, `reverted` ou `conflicted`; les suggestions échues deviennent `ignored` ou `expired`. Nouveau résumé qualité séparé `GET /restaurants/:id/service-copilot/telemetry-summary`. Migration Prisma `20260722000003_copilot_telemetry` créée mais non appliquée ; en production, `SERVICE_COPILOT_TELEMETRY_SECRET` (≥32 caractères) est requis. Tests API ciblés 76/76 verts ; déploiement non exécuté.

2026-07-22 00:57 — [voice, api, ops] **Telnyx & Deepgram STT en production (VPS pmbtc)** — Résolution de deux problèmes bloquant les appels entrants en production (`+33 4 51 22 15 28`) : 1) **BullMQ Job ID Sanitization** : Remplacement des deux-points (`:`) par des tirets bas (`_`) dans les identifiants de jobs BullMQ (`sanitizeJobId`), évitant le crash de validation de BullMQ v5.76 (`Error: Custom Id cannot contain :`) lors de la réception du webhook `call.initiated` de Telnyx. 2) **Connexion STT Deepgram Flux** : Ajout de l'appel explicite à `connectDeepgramFlux(session)` lors de l'évènement `start` du stream WebSocket Telnyx (`/voice/stream/:callId`) et évaluation runtime de `DEEPGRAM_API_KEY`, permettant au processeur STT d'ouvrir le canal WebSocket Deepgram et de transcrire l'audio du correspondant après le message d'accueil. 1314/1314 tests API et 116/116 tests Dashboard validés, déploiement VPS réussi (`8bda9a97`).

2026-07-22 00:50 — [api, service-copilot] **Idempotence stricte de l’application d’un plan** — `delay-impact/apply` calcule une empreinte SHA-256 stable des champs métier confirmés (réservation, retard, table alternative, entrée d’attente, acceptation, rapport vocal) et la conserve dans l’audit append-only de récupération. Même clé + même plan : retour idempotent du premier résultat ; même clé + plan divergent : 409 explicite sans aucune mutation. La comparaison se fait avant le préflight et de nouveau après les verrous transactionnels afin de couvrir les requêtes concurrentes. Compatibilité lecture avec les audits antérieurs sans empreinte. Aucune migration. API ciblée 57/57, typecheck et lint verts.

2026-07-22 00:41 — [api, dashboard, service-copilot] **Service Copilot V1 : pouls opérationnel Live service** — Nouveau `GET /restaurants/:id/service-copilot/pulse?date=YYYY-MM-DD`, calculé dans le fuseau du restaurant. Il regroupe retards confirmés, arrivées à installer, arrivées dans les 30 minutes, tables réellement en service et liste d’attente. Le bandeau compact au-dessus du plan distingue `urgent`, `à surveiller` et `sous contrôle` ; pour une date hors temps réel il affiche uniquement une synthèse, sans faux signal d’action. Lecture seule, aucune automatisation de message, aucune migration ni changement de droits. API ciblée 42/42, dashboard 29/29, typecheck et lint API/dashboard/CSS verts.

2026-07-22 00:31 — [product, auth, service-copilot] **Décision d’accès** — Pas de rôles ni de permissions différenciées pour le moment : tous les utilisateurs authentifiés d’un même compte restaurant disposent des mêmes accès, notamment l’application et l’annulation des plans Service Copilot. Les audits identifient l’opération, mais ne bloquent pas l’action selon un rôle.

2026-07-22 00:27 — [api, dashboard, service-copilot] **Service Copilot V1 : historique persistant des plans de retard** — Nouveau `GET /restaurants/:id/service-copilot/delay-recoveries` : l’API reconstitue, sans migration, les opérations depuis les audits append-only puis les rapproche de l’état courant. Chaque plan est classé `applied`, `reverted` ou `blocked`, avec une raison métier lorsque l’annulation automatique n’est plus sûre. Le Live service affiche jusqu’à trois plans récents et permet de rouvrir « Annuler » après rafraîchissement ou reconnexion ; l’endpoint transactionnel reste l’autorité finale et revalide les conflits. Tests ciblés API 49/49 et dashboard 28/28, typecheck et lint API/dashboard/CSS verts.

2026-07-22 00:05 — [api, dashboard, service-copilot] **Service Copilot V1 : « Annuler ce plan » transactionnel** — `apply` conserve désormais dans l’audit le snapshot complet avant/après et retourne un `operationId`. Le nouvel endpoint authentifié `POST /restaurants/:id/service-copilot/delay-impact/revert` verrouille les deux réservations, l’entrée de liste d’attente et les deux tables ; il restaure la réservation retardée, annule la réservation promue et remet l’entrée en `PENDING` uniquement si tout est inchangé et la table initiale libre. Sinon 409 sans écriture partielle. Retour arrière idempotent et audité (`reservation_delay_recovery_reverted`, `reservation_cancelled`, `waiting_list_restored`). Dashboard : confirmation « Annuler ce plan », avertissement que les communications ne sont pas rappelées, puis résultat « Plan initial restauré ». Aucune migration. Tests ciblés API 42/42, dashboard 26/26, typecheck et lint API/dashboard/CSS verts ; suite API globale 1 288 tests verts hors 11 WebSocket `listen EPERM` connus.

2026-07-21 23:52 — [ops, production, service-copilot] **Smoke simulé complet et nettoyé** — Sur `Test Restaurant`, création d’un `reservation_delay_reported` explicitement marqué `voice_simulation`, apparition vérifiée de la recommandation Martin +25 min, analyse déterministe T4 → T12 / Alice liste d’attente → T4 et génération de deux brouillons `review-required` bloqués par absence de consentement. Application transactionnelle réussie puis rejouée avec `idempotent: true` et le même identifiant de réservation promue. Preuves : exactement 1 `reservation_delay_reported`, 1 `reservation_delay_recovered`, 1 `waiting_list_promoted`, 0 message avant/après et aucun appel supplémentaire. Nettoyage non destructif effectué : réservations/entrée annulées, tables/plan de smoke désactivés, audits append-only conservés. Ce test ne remplace pas encore le smoke avec véritable appel ni une consultation Sentry externe.

2026-07-21 23:39 — [ops, production, service-copilot] **Smoke métier préparé sur le restaurant de test** — Le restaurant production `Test Restaurant` (`test-resto-1`), sans réservation ni plan existant, a reçu un plan isolé T4/T12 et deux données fictives pour le 22 juillet à 19 h 30 : `Martin Test Copilot` confirmé sur T4 et `Alice Test Copilot` en liste d’attente. La simulation déployée propose exactement Martin T4 → T12 avec 25 minutes de retard puis Alice → T4. Le numéro Telnyx actif a été confirmé dans l’inventaire ; l’appel réel, l’application, la vérification des audits/envois et le nettoyage restent à exécuter. Nouveau runbook `docs/runbooks/service-copilot-pilot.md` : sécurité, preuves, retour arrière manuel, shadow mode et métriques.

2026-07-21 23:29 — [database, api, dashboard] **Base de données : synchronisation du schéma `floor_plans` (`is_active` / `is_default`)** — Exécution de `prisma db push --accept-data-loss` et `pnpm db:seed`. Résolution de l'erreur `PrismaClientKnownRequestError` (`column floor_plans.is_active does not exist`). Les endpoints d'API `/restaurants/:id/floor-plans` retournent désormais 200 OK et le dashboard charge correctement sur `http://localhost:3000/dashboard/floor-plan`.

2026-07-21 23:28 — [dashboard] **Floor-plan : icônes de fauteuils 2D architecturales avec orientation dynamique** — Remplacement des simples cercles de chaises par un composant SVG `FloorPlanArmchairIcon` reproduisant le rendu vectoriel d'un fauteuil d'architecte 2D (dossier galbé, coussin d'assise, coutures d'accoudoir et encoches de structure). Calcul dynamique de l'orientation radiale et par bord de table (`top`: 0°, `bottom`: 180°, `left`: 270°, `right`: 90° et radiale pour tables rondes), orientant chaque assise directement vers le centre de la table. Tests unitaires 26/26 verts.

2026-07-21 23:02 — [voice, api, dashboard] **Service Copilot : flux retard durci après audit des 11 incohérences** — `reportDelay` convertit désormais l’heure locale avec le fuseau du restaurant. La recommandation transporte la date de service et l’identifiant d’audit ; l’application est idempotente même sans clé cliente et refuse toute promotion sans confirmation explicite que le groupe est présent et accepte la table. Simulation et transaction partagent les contraintes table/plan actifs, `minCapacity` et section préférée ; une entrée dont le créneau a commencé est proposée à partir de maintenant, jamais dans le passé. Dashboard : changement de retard invalide le plan, les brouillons remplacent atomiquement leur impact recalculé, le dialogue bloque double clic/fermeture, l’URL vocale est nettoyée après succès et le résumé final conserve noms et mouvements avec statut « Communication requise ». Les appels vocaux successifs et réservations absentes de la date sont gérés explicitement. Aucune migration. API 1282/1288 tests verts (6 skips, test WebSocket local exclu car `listen EPERM` sandbox), dashboard 113/113, typecheck, lint API/dashboard et lint CSS verts. QA navigateur limitée par la `PrismaClientKnownRequestError` locale existante (aucun seed forcé).

2026-07-21 22:39 — [dashboard] **Service Copilot : brouillons visibles dans Live service** — Après une simulation de retard faisable, le responsable peut ouvrir « Préparer les messages ». Les cartes affichent le brouillon, l’éligibilité SMS/e-mail ou le blocage de consentement/contact, et rappellent explicitement qu’aucun envoi automatique n’a lieu. Aucun bouton d’envoi n’est présent.

2026-07-21 22:38 — [dashboard] **Service Copilot : provenance des groupes explicite** — Le plan de retard ne présente plus le groupe promu comme s’il changeait de table : la carte affiche désormais « Liste d’attente → T4 » et précise que le client n’avait pas encore de table. La réservation retardée montre son mouvement complet « Martin · T4 → T12 ». Tests canvas 23/23 et typecheck dashboard verts.

2026-07-21 22:35 — [dashboard] **Service Copilot : lecture rush du retard vocal** — La bannière Live service est restructurée autour de trois informations immédiatement scannables : appel reçu (client, couverts, retard), deux mouvements proposés sous forme de cartes « table libérée maintenant » / « à son arrivée », puis action unique « Appliquer ce plan ». Les tables concernées sont mises en évidence, la confirmation nomme les deux changements et le succès affiche l’action humaine restante. Les actions « Installer à table » / « Libérer la table » bloquent les doubles clics pendant la mise à jour. Tests canvas 23/23, typecheck, lint et lint CSS verts.

2026-07-21 22:25 — [dashboard] **Service Copilot : UX pilote recentrée sur le terrain** — Abandon volontaire, avant pilote, de la télémétrie/outbox généralisée envisagée pendant la revue produit : le besoin n’est pas encore validé. Le retard signalé par la voix affiche désormais son analyse et le plan sûr directement au-dessus du canevas Live service, avec confirmation transactionnelle existante. Les actions deviennent « Installer à table » et « Libérer la table ». La confirmation et le retour de succès rappellent qu’aucun message externe n’est envoyé automatiquement depuis ce flux. Tests canvas 23/23, typecheck et lint dashboard verts ; aucune migration ni nouvelle route.

2026-07-21 22:21 — [api] **Service Copilot : éligibilité transactionnelle des brouillons** — Les brouillons de retard vérifient désormais le contact et le consentement transactionnel le plus récent, limité au restaurant. Ils n’indiquent `sms` ou `email` que si le consentement correspondant est actif ; sinon ils retournent explicitement `no-contact` ou `no-transactional-consent`. Le statut demeure `review-required` et aucun envoi n’existe.

2026-07-21 22:16 — [api] **Service Copilot : brouillons de communication calculés** — `POST /restaurants/:id/service-copilot/delay-impact/drafts` réexécute la simulation read-only puis retourne des brouillons déterministes pour la réservation retardée et l’entrée de liste d’attente, basés sur les heures client arrondies. Chaque brouillon est `review-required` : aucun contact, canal ni envoi n’est lu ou exécuté. Une prochaine phase séparée vérifiera le consentement transactionnel avant de proposer un SMS/e-mail.

2026-07-21 22:10 — [api, dashboard] **Service Copilot : heures client arrondies** — Nouvelle règle déterministe `toCustomerFacingTime` : toute heure destinée à un client est arrondie vers le haut au créneau de 5 minutes suivant, afin de ne jamais promettre plus tôt que le calcul opérationnel (20:08 → 20:10). Le simulateur de retard expose cette heure distincte de l’heure interne exacte ; elle servira de source aux futurs brouillons de communication.

2026-07-21 21:10 — [dashboard] **Service Copilot : confirmation d’équilibrage serveur** — Une recommandation `server-rebalance` n’exécute plus le PATCH directement : le cockpit affiche une confirmation nommant table et serveur cible, avec rappel de prévenir l’équipe. La décision reste entièrement humaine.

2026-07-21 21:05 — [api, dashboard] **Service Copilot : équilibrage dynamique des serveurs** — Les recommandations analysent les tables actives affectées et les couverts `CONFIRMED`/`SEATED` des 30 prochaines minutes. En cas d’écart significatif, le cockpit propose de confier une table confirmée d’un serveur surchargé au serveur le moins chargé. Aucune réaffectation automatique : le responsable déclenche explicitement le PATCH existant, puis le Copilot se rafraîchit.

2026-07-21 20:45 — [voice, api, dashboard] **Service Copilot : retard vocal soumis à validation humaine** — L’outil vocal `reportDelay` exige nom, date, heure et minutes de retard, identifie une réservation `CONFIRMED` exacte puis écrit uniquement l’événement append-only `reservation_delay_reported` (corrélé à l’appel). Le cockpit le remonte comme action prioritaire et ouvre directement l’analyse d’impact de la réservation. Aucun déplacement de table, promotion de file ou message à un tiers n’est exécuté depuis l’appel ; toute récupération reste soumise à la confirmation transactionnelle du responsable.

2026-07-21 20:37 — [api, dashboard] **Service Copilot : application confirmée d’un plan de retard** — Le panneau Live service propose désormais « Confirmer et appliquer » après une simulation faisable. `POST /restaurants/:id/service-copilot/delay-impact/apply` refait un préflight, verrouille la réservation, l’entrée de liste d’attente et les deux tables, puis revalide les conflits dans une transaction avant de décaler/réassigner le client en retard et de promouvoir le groupe en attente. Les deux mutations sont auditées (`reservation_delay_recovered`, `waiting_list_promoted`). Un changement d’état retourne 409 ; aucun message externe n’est envoyé. Tests API route/service 33/33, canvas 21/21, typecheck et lint verts.

2026-07-21 20:32 — [api, dashboard] **Service Copilot : simulation d’impact d’un retard** — Endpoint read-only `POST /restaurants/:id/service-copilot/delay-impact` et panneau Live service « Retard annoncé ». Pour une réservation CONFIRMED assignée, le moteur cherche une table alternative à la nouvelle ETA, puis vérifie que la table initiale peut accueillir une entrée PENDING de liste d’attente en excluant seulement la réservation retardée. Il retourne un plan seulement si les deux contrôles passent ; aucune mutation, promotion ou message n’est exécuté. L’UI rappelle que le plan doit être validé et revalidé sous verrou à l’exécution. Tests API/service/route 44/44 et canvas dashboard 21/21 verts.

2026-07-21 20:25 — [api, dashboard] **Service Copilot : prévision de libération en shadow mode** — `ServiceTurnPredictionService` dérive les durées réelles depuis les événements append-only `reservation_seated` → `reservation_honored` sur 90 jours (lecture bornée à 5 000 événements). Le Copilot privilégie la médiane par table et taille de groupe (≥ 6 services), sinon celle du restaurant (≥ 12), puis conserve explicitement la durée configurée si l’historique est insuffisant. Les recommandations indiquent source, échantillon et confiance ; aucune action n’est automatisée ni aucune migration nécessaire. Tests Copilot 12/12, typecheck API/dashboard et lint dashboard verts.

2026-07-21 20:05 — [dashboard, e2e] **Service Copilot : démo E2E déterministe** — Le test Playwright du plan de salle simule désormais, sans API ni données staging, les trois réponses du Copilot : table disponible (et absence de refus contradictoire), section Terrasse pleine avec bascule vers Salle, et restaurant complet avec prochain créneau. Le sélecteur de section est exercé. Les 12 E2E (iPhone, iPad, desktop), typecheck et lint dashboard sont verts.

2026-07-21 19:42 — [api] **Service Copilot : refus uniquement en fallback** — Une simulation faisable affichait aussi le scénario contradictoire « Aucune table disponible ». `ServiceCopilotSimulationService` ne recherche et ne retourne désormais le prochain créneau / refus que si aucun placement direct ou changement de section n’est possible. Tests floor-plan ciblés 43/43 et typecheck API verts ; en attente de CI/staging.

2026-07-21 19:28 — [api] **Correctif allocation : bornes `tsrange` Prisma** — Les simulations Service Copilot échouaient sur staging : Prisma lie les `Date` des `$queryRaw` en `timestamptz`, incompatible avec `tsrange(timestamp, timestamp)`. `TableAllocationService` convertit désormais explicitement les deux bornes en timestamp UTC, côté réservations et holds, sans migration. Régression couverte par un test des deux requêtes. Tests floor-plan ciblés 43/43 et typecheck API verts. Correctif `1c215f7` en attente de CI/staging.

2026-07-21 18:45 — [api, dashboard] **Service Copilot : simulation what-if** — `ServiceCopilotSimulationService` ajoute `POST /restaurants/:id/service-copilot/simulate` pour tester les 3 scénarios (direct, changement de section, refuse + prochain créneau) sans mutation. Dashboard : widget `ServiceCopilotSimulator` dans `floor-plan` en mode `service-live` avec formulaire, résultats explicables et actions `link`. Tests service 3/3, typecheck et lint API/dashboard verts. Aucune migration DB.

2026-07-21 17:45 — [api, dashboard] **Sokar Service Copilot MVP** — Moteur de recommandations déterministes dans `ServiceCopilotService` : réservations `CONFIRMED` en retard (> 15 min, `critical` si > 30 min), tables `SEATED` bientôt libres dans les 15 min, et entrées `PENDING` de file d’attente compatibles avec une table dans les 30 min. `GET /restaurants/:id/service-copilot/recommendations` retourne jusqu’à 3 suggestions triées, sans mutation. Widget cockpit `ServiceCopilotWidget` affiché en premier sur le dashboard, avec cartes colorées par priorité (`destructive`/`warning`/`info`) et actions `link`/`api` vers les endpoints transactionnels existants. Ajout du token `info` dans `globals.css` et `tailwind.config.js`. Tests service 8/8, API 1058/1064 (1 skip), dashboard 105/105, lint et typecheck verts. Poussé sur `main`.

2026-07-21 14:15 — [dashboard] **Floor-plan : liste d’attente dans Live service** — Le cockpit expose désormais `Plan | Liste d’attente | Statistiques`. Le polling 10 s récupère en parallèle les réservations et les seules entrées `PENDING` à la date du service ; une panne de file n’empêche pas le plan de se rafraîchir. Chaque carte affiche position, client, couverts, créneau et préférence de section ; « Proposer une table » appelle la promotion autoritaire côté API puis re-fetch le cockpit. Aucune migration ni route nouvelle. Tests canvas 21/21, typecheck, lint et build dashboard verts.

2026-07-21 13:43 — [dashboard, ci] **E2E floor-plan autonome** — La CI injecte les flags démo baked `NEXT_PUBLIC_DEMO_RESTAURANT_ID=e2e-demo-restaurant` et `NEXT_PUBLIC_DEMO_STAGING=true`, évitant le redirect Clerk en build de production. `floor-plan.spec.ts` intercepte les réponses `/api/proxy` nécessaires (liste, plan, réservations) : aucun Fastify n’est requis pour ce scénario. Reproduction CI locale : build standalone + E2E 12/12, dashboard 103/103, lint et typecheck monorepo 12/12 verts.

2026-07-21 13:05 — [api, dashboard] **Floor-plan : fallback de section + raccourcis d’édition** — `TableAllocationService` retombe sur les tables du plan par défaut quand la section préférée n’offre aucune table compatible ; la section reste prioritaire. En édition, Delete confirme la suppression des tables sélectionnées, Backspace supprime le mur sélectionné, et les flèches déplacent les tables sélectionnées de 1 px (10 px avec Shift), avec bornes canvas et historique undo/redo. Tests ciblés API 55/55, dashboard 103/103, lint API/dashboard et typecheck monorepo 12/12 verts.

2026-07-21 12:31 — [api, dashboard] **Floor-plan : allocation explicable + undo/redo géométrique** — `suggest-table` prévisualise désormais jusqu’à trois tables compatibles, avec score et raisons, sans verrou SQL ; les champs legacy `tableId`/`reason` restent pour compatibilité. Le canvas garde 50 snapshots strictement géométriques pour déplacer/redimensionner/faire pivoter tables et murs, aligner/répartir les tables et modifier longueur/angle des murs ; boutons Annuler/Rétablir et raccourcis ⌘/Ctrl+Z, ⌘/Ctrl+⇧Z. Les versions de mutation par table/mur ignorent les réponses réseau périmées après undo/redo ; suppression d'une table/mur purge l'historique pour éviter toute recréation fantôme. Aucune migration. Tests dashboard 100/100, lint dashboard et typecheck racine 12/12 verts.

2026-07-20 23:22 — [api] **Waiting list P3.3 : notifications de promotion (SMS/email)** — Worker BullMQ dédié `waiting-list-promote` + queue `waitingListPromote` ; `promoteEntry` planifie deux jobs (`channel: 'sms'` et `'email'`) après commit de la transaction avec `jobId` déterministe. Le worker vérifie que l'entrée est `PROMOTED`, récupère la réservation promue et envoie SMS/email si contact présent. Copy user-facing en `vous` avec restaurant, date, heure, couverts et téléphone du restaurant. `REDACT_PATHS` étendu (`*.email`, `*.customerEmail`, `*.customerPhoneNormalized`) ; `env.ts` documente les vars SMTP optionnelles. Tests 12/12 worker + 17/17 service, full API 1235/1241 (1 skip), typecheck + lint verts.

2026-07-20 18:39 — [dashboard] **Dashboard : normalisation couleurs via design tokens** — Remplacement des classes Tailwind de couleurs fixes (cyan, amber, emerald, red, purple, blue, orange) par les tokens du design system (brand, success, warning, destructive, metal) dans `GaugeDial`, `MobileBottomNav`, `MobileDataCard`, `gift-card-list` et `onboarding-dashboard`. Typecheck + lint dashboard verts.

2026-07-20 22:42 — [api, dashboard, connect] **Waiting list P3.2 : UI dashboard, widget Connect, review fixes et migrations** — Dashboard `/dashboard/reservations` : onglets Réservations/File d’attente, liste avec date picker, promote/remove et badges de statut. `/dashboard/settings` : toggle file d’attente + max par créneau. Connect `booking-widget` : offre liste d’attente sur hold 409, formulaire de join, affichage position/token, annulation. Routes API publiques `POST/DELETE /public/r/:slug/waiting-list` et admin `GET/POST promote/DELETE /restaurants/:id/waiting-list`. Fix review : ajout colonne `source` dans `WaitingListEntry`, `promoteEntry`/`cancelByToken`/`cancelByStaff` transaction-safe avec `FOR UPDATE`, migration dédiée `reservation_overlap_exclusion`, recalc positions `20260723000002_recalc_waiting_list_positions`. Tests API 1220/1226 verts (1 skip), dashboard 22/22, connect 95/95. Lint + typecheck verts.

2026-07-20 21:56 — [api] **Waiting list service layer (P3.2)** — `WaitingListService` avec `join`, `cancelByToken`, `promoteEntry`, `expireEntry`, `cleanupExpired`, `list`, `findCompatibleCandidates` ; helper `normalizePhone` dans `@sokar/shared` ; queues BullMQ `agentic-waiting-list-expire` et `waiting-list-cleanup` avec workers planifiés toutes les 5 min. Migrations `add_waiting_list_position` et `add_reservation_customer_email`. Typecheck API, shared tests 46/46, waiting-list tests 12/12 verts.

2026-07-20 20:31 — [api] **Multi floor-plan support** — Schema `FloorPlan` : suppression de l’unicité sur `restaurantId`, ajout des colonnes `isDefault`/`isActive` (défaut `true`) et index partiels. Migration `20260721000000_multi_floor_plan` crée colonnes, backfill, index uniques partiels et drop ancien index. `FloorPlanService` expose `getDefaultFloorPlan` (crée si absent), `listFloorPlans`, `getFloorPlanById`, `createFloorPlan`, `updateFloorPlanById`, `deleteFloorPlan` ; `getOrCreateFloorPlan` reste wrapper compatibilité. Toutes les méthodes de gestion (sections, tables, murs, planning) acceptent un `floorPlanId` optionnel et se basent sinon sur le plan actif par défaut. `TableAllocationService.allocate` privilégie `preferredSectionId` sinon charge le plan par défaut. `CapacityAwareAvailabilityService.getAvailability` filtre par `preferredSectionId` ou charge tous les plans actifs. Routes `GET/POST /restaurants/:id/floor-plans`, `GET/PATCH/DELETE /restaurants/:id/floor-plans/:floorPlanId` ajoutées ; routes `/restaurants/:id/floor-plan` restent compatibles. `/public/widget/:slug` retourne les sections du plan par défaut actif. Tests floor-plan 86/86, connect/widget/hold 41/41, full API 1195/1201 verts (1 skip). Typecheck API + database verts.

2026-07-20 20:15 — [api] **P2 floor-plan : tsrange Postgres pour l’allocation de tables** — `TableAllocationService` remplace les `findFirst` Prisma par des `$queryRaw` utilisant `tsrange(starts_at, ends_at) && tsrange($start, $end)` pour `reservations` et `tsrange(slot_start, slot_end) && tsrange(...)` pour `agentic_holds`. Migration `add_tsrange_indexes` créant deux index GiST partiels (sans `btree_gist`, sans `NOW()` dans les prédicats). `isTableAvailable` reste inchangée publiquement ; tests `table-allocation` mis à jour pour mocker `$queryRaw`. Typecheck API, tests floor-plan 57/57, hold 10/10, connect routes 19/19 verts.

2026-07-20 19:30 — [dashboard] **P1 floor-plan : alertes dans StatsPanel** — `FloorPlanCanvas.tsx` : ajout d’une section "Alertes" dans `StatsPanel` (mode service) pour afficher les réservations sans table (`tableId` null) et les créneaux en surcapacité (couverts > capacité totale). Utilise les tokens `warning`/`destructive`. Ajout de 6 tests dans `FloorPlanCanvas.test.tsx` couvrant l’affichage, l’exclusion `CANCELLED`/`NO_SHOW` et la pluralisation. Typecheck, lint, tests dashboard 85/85 verts.

2026-07-20 19:00 — [connect] **P2 floor-plan : préférences de section pour le client** — `BookingWidget` affiche un sélecteur d’emplacement (section du plan de salle) quand le restaurant a des sections. `GET /public/r/:slug/availability` et `POST /public/r/:slug/hold` acceptent `preferredSectionId`. `CapacityAwareAvailabilityService` filtre d’abord les tables de la section préférée puis retombe sur les autres. `HoldService` passe la préférence à `TableAllocationService.allocate`. `/public/widget/:slug` renvoie les sections du floor plan. Typecheck API + connect, tests API 1185/1191, connect 108/108 verts.

2026-07-20 18:30 — [dashboard] **P2 floor-plan : onglet Statistiques en Live service** — `FloorPlanCanvas.tsx` : ajout d’un toggle Plan / Statistiques en mode service. `StatsPanel` affiche le taux d’occupation, les couverts prévus, les tables actives/inactives, les compteurs de statuts (en service, prochaines, retard) et une prévision des couverts par créneau de 30 min. Typecheck, lint, tests 79/79, e2e floor-plan desktop-1440 verts.

2026-07-20 18:00 — [dashboard] **P2 floor-plan : drag-and-drop réassignation réservation** — `FloorPlanCanvas.tsx` : la réservation d’une table devient draggable en mode service, les tables deviennent des zones de drop, drop sur une autre table appelle l’API `assign-table`. Overlay visuel pendant le drag. Typecheck, lint, tests 79/79 et e2e `floor-plan.spec.ts` desktop-1440 verts.

2026-07-20 17:10 — [dashboard] **Test E2E /dashboard/floor-plan** — Création de `apps/dashboard/e2e/floor-plan.spec.ts`, configuration du mode demo local (`apps/dashboard/.env.local` + `DEMO_RESTAURANT_ID`), redémarrage de `pnpm dev`. Test Playwright 3/3 verts (iphone-14, ipad-mini, desktop-1440). Typecheck et lint dashboard verts.

2026-07-20 17:00 — [dashboard] **Badge Sans table uniquement sur réservations actives** — Correction de `ReservationsPage` : le badge `Sans table` et le bouton/action `Allouer` ne s’affichent plus que pour les statuts `CONFIRMED`/`SEATED`; les autres statuts (`CANCELLED`, `NO_SHOW`) affichent `—` quand aucune table n’est allouée. Ajout d’un test couvrant le cas `CANCELLED` sans `tableId`. Typecheck, tests ciblés et lint dashboard verts.

2026-07-20 16:00 — [api] **Suppression ConnectAvailabilityService mort + lint API vert** — Suppression de `apps/api/src/modules/connect/availability.service.ts` (code non utilisé) ; Connect/route `/public/r/:slug/availability` utilisent désormais `CapacityAwareAvailabilityService` (floor-plan). Nettoyage lint API : suppression de l’import inutilisé `TableAllocationError` dans `floor-plan.service.ts` et `/* eslint-disable no-console */` dans `scripts/backfill-reservation-tables.ts`. Mise à jour des docs (`docs/positioning-vs-zenchef.md`, `docs/PROJECT_MAP.md`, `docs/floor-plan-spec.md`, `docs/connect-v1.1.md`) et du commentaire `floor-plan/availability-capacity-aware.service.ts`. Typecheck API, tests connect/floor-plan et lint global API verts.

2026-07-20 15:00 — [dashboard] **FloorPlanCrud tests d’erreur + sélecteurs robustes** — `FloorPlanCrud.tsx` : ajout `data-testid="section-card"`, rollback d’état sur toggle/renommage en cas d’erreur API. `FloorPlanCrud.test.tsx` : remplacement de `.closest('.sokar-card')` par `getAllByTestId('section-card')`, 5 tests d’erreur ajoutés (suppression/renommage section, création/suppression/toggle table), total 16 tests. Typecheck, `FloorPlanCrud.test.tsx`, `page.test.tsx` et lint dashboard verts.

2026-07-20 13:12 — [api] **Backfill réservations legacy v3 (final)** — `apps/api/scripts/backfill-reservation-tables.ts` finalisé après reviews : pagination par lots `--batch-size` (offset en dry-run, filtre `tableId: null` en mode normal), dry-run en lecture seule via `TableAllocationService.allocate(..., { readOnly: true })`, recalcul optionnel `endsAt` avec `--force-ends-at`, logs `--verbose`, validation CLI, timeout 60s. Review finale `Approve`. `TableAllocationService` supporte `options.readOnly`. Tests floor-plan 24/24 verts.

2026-07-20 11:21 — [dashboard] **Switch desktop /dashboard/floor-plan** — Ajout d'un toggle "Live service" / "Salle édition" dans `page.tsx` pour basculer entre les modes `service` et `design` de `FloorPlanCanvas` via `router.replace` (conservation des query params, pas de rechargement). Tests `page.test.tsx` créés (5 tests). Typecheck, tests et lint dashboard verts.

2026-07-20 11:11 — [api] **Durcissement SEATED / walk-in (review fixes)** — Mapping HTTP des `TableAllocationError` harmonisé (404 `TABLE_NOT_FOUND`, 409 `TABLE_NOT_AVAILABLE`, 400 autres) sur les routes `state` et `walk-ins`. Commentaire ajouté sur `statusForState` pour documenter les états sans équivalent enum. Tests `assertTableAvailableForSeating` complétés : exclusion de la réservation courante, table inactive, capacité/minCapacity. Correction d'une variable non définie dans le mock `makeMockPrisma`. Typecheck + 46 tests ciblés verts.

2026-07-20 11:04 — [api] **Durcissement SEATED / walk-in** — `TableAllocationService.assertTableAvailableForSeating` vérifie l'existence, l'activité, la capacité, le verrou `FOR UPDATE` et la disponibilité créneau. `ReservationService.transitionState` appelle cette méthode pour `SEATED`, synchronise `status`, et refuse les réservations terminées. `FloorPlanService.createWalkIn` utilise la même assertion dans la transaction. Routes renvoient 409 pour `TableAllocationError` et erreurs state-machine. Tests des services et routes mis à jour. Typecheck + tests floor-plan verts.

2026-07-20 10:43 — [dashboard] **Phase 1 floor-plan live : polling robust AbortController, heartbeat relatif, re-fetch croisé après actions** — `FloorPlanCanvas.tsx` : `pollAbortRef`/`pollInFlightRef`, `loadReservations({ force?: boolean })`, abort/stale-response guard, polling 10 s avec gestion `document.hidden`, re-fetch après `updateReservationState`, `createWalkIn` et `assignTable` ; composant `ElapsedSince` affiche "mis à jour il y a Xs". Typecheck dashboard + tests `FloorPlanCanvas` + tests API floor-plan verts.

2026-07-17 23:19 — [dashboard, docs, agent] **Refonte dashboard pilotage + floor-plan build tools + staging demo mode + vault audit** — floor-plan: outils de construction directs + persistance dimensions/rotation (623a93f), fixes repositionnement chaises (4a9ea54, d6c083d) ; staging: demo mode auto-connecté sans login isolé prod (3d5d3a4) ; dashboard: refonte UI pilotage + fallback dev Clerk + i18n (16d8e68), bypass Clerk login local demo (4a6e798) ; vault: rewire Obsidian cross-session memory (376f29a), audit Sokar Connect actif + Context/TODOs clos (af53248), archive notes legacy Canal A (f5f23b6), session telnyx audit vps (7ab81d7) ; agent: fix tools/hermes template config api_mode chat_completions + skills_dir repo-root (5e1759a). (cf. [[Sokar Connect P0]], [[Telnyx Pipeline]], [[Hermes Obsidian Integration]])
2026-07-17 14:00 — [docs, agent] **Rewiring Obsidian vault as Hermes cross-session memory** — `OBSIDIAN_VAULT_PATH` set in `~/.hermes/.env` → `docs/obsidian`. AGENTS.md now mandates reading `Context.md` + tail of `Journal.md` at session start, and updating the vault after any significant change (obsidian-doc skill). Stalé notes corrected: Next.js 14→15, React 18→19, glm-5.2→minimax-m3, OpenRouter deepseek→minimax-m3. (cf. [[Hermes Obsidian Integration]])
2026-06-24 18:44 — [connect, test] **Stabilisation Sokar Connect + fix voice mocks** — Ajouté 12 tests (7 villes T7 + 5 analytics T8) → **38/38 tests Sokar Connect verts**, 1 skip intentionnel (smoke e2e). Typecheck API + Sokar Connect exit 0. Cause du timeout 18:00 identifiée : `tsserver.js` (VSCode LSP) qui consomme 100% CPU → fix via `find ... -name "*.tsbuildinfo" -delete`. Bonus : 2 tests voice régressés par autre dev (`CustomerService.buildReturningGreeting`/`recordCallActivity` mocks manquants dans `telnyx.pipeline.test.ts`) → fixés, mais 17 autres tests voice restent cassés (pré-existants, hors scope Sokar Connect, loggés dans TODOs comme dette technique). Build Sokar Connect incrémental non re-testé (tsserver bloque le build process — à rebooter VSCode). **Tests verts, prêt pour le pilote**. Prochain : reboot IDE + cleanup tsserver.

## Décisions récentes

- **2026-07-21** — Floor-plan : la liste d’attente est une vue Live service dédiée et ne permet pas de forcer une table depuis le client. La promotion utilise exclusivement l’allocation transactionnelle existante côté API ; le drag-and-drop vers une table est différé.
- **2026-07-21** — Floor-plan : l’allocation expose d’abord trois propositions explicables et read-only ; undo/redo reste volontairement borné à la géométrie réversible du canvas (50 actions locales, pas de création/suppression). La machine à états de service complète reste différée aux retours de pilotes.
- **2026-06-24** — Sokar Connect v1.1 validée par Hamza (8 corrections v1→v1.1 intégrées : static export→standalone, suppression basePath, VPS+Caddy+Cloudflare proxy, gating corrigé, source de vérité unique, seed local/staging, audit log métier only, CORS durci). Spec : [[Sokar Connect P0]]
- **2026-06-24** — Phase 0 Sokar Connect lancée : ordre T1→T2+T3→T4 avec STOP revue entre chaque. T1 (`migrate deploy`) **fait le 2026-06-24** (backfill Chez Sokar → Lyon/69001/FR), T2-T10 (API publique, JSON-LD, app connect, pages, analytics, sécurité) écrits et testés (38/38 tests verts). Reste : T4 app en prod + P1 pilote fermé (cf. [[Sokar Connect P0]]).
- **2026-06-24** — Cleanup docs/ : `pilot/_archive/` créé, `runbook.md` extrait de `pilot/`, 3 versions agentic obsolètes archivées. Vault Obsidian re-activé après 5 semaines d'arrêt (cf. Journal).
- **2026-06-23** — Cleanup Vault Obsidian : 17 notes. Pas alimenté depuis 2026-05-21. Rétro-documentation des 5 semaines sautées : agentic-reservations P0, MCP OAuth, OpenAI Reserve, model switch Hermes, VSCode crashes. Reprise automatic à partir de T1 Sokar Connect.
- **2026-06-23** — Model switch Hermes : `glm-5.2` → `minimax-m3` via `opencode-go`. Supervisor `glm-5.2=review`. Crof.ai supprimé (404 sur `deepseek-v4-pro`). Contrainte : `/v1/messages` exige `x-api-key` (Hermes n'envoie pas) → `api_mode: anthropic_messages` désactivé.
- **2026-06-22** — RGPD Phase 5 durci : three-token pattern (OTP → verification token → one-shot action) sur `/api/rgpd/erase` et `/api/rgpd/export`. Modèles `IdentityVerificationOtp` + `SignedTokenUsage`. Phase 7 a ajouté identity hardening.
- **2026-06-21** — Agentic-reservations P0 livré : hold/quote state machine, partial unique index `one_active_hold_per_slot`, idempotency scoped, audit log append-only, RGPD structured consents. Spec v3.2 = référence prod-safe.
- **2026-06-21** — Migration TTS ElevenLabs → Cartesia Sonic 3.5 finalisée (cf. [[Telnyx Pipeline]]). Limitation speed/volume sur sonic-3.5 (depuis avril 2026), workaround = revenir à sonic-3 si besoin.
- **2026-05-21** — Crof AI `deepseek-v4-pro` ne fonctionne pas comme provider `custom` (404). Le provider normalise le model name en `deepseek/deepseek-v4-pro` que Crof rejette. **Résolu** par switch vers `opencode-go` + `minimax-m3` (cf. 2026-06-23).
- **2026-05-21** — Clerk mocké en test via `helpers.ts` (`requireOrg` lit `Authorization` header). Telnyx webhook guard mocké dans `reservation.test.ts`.

## TODOs actifs

- [ ] **Sokar Connect — Pilote fermé** : 10 restos réels (en plus de chez-sokar-demo) + monitoring Prometheus + 1 vraie résa via lien ChatGPT → go/no-go chiffré (cf. [[Sokar Connect P0]] §22 Phase 1 critères)
- [x] **Dette technique — Voice tests cassés (17 failures pré-existantes)** : résolu — `vitest run` API : 120 test files pass, 1235 tests pass, 6 skipped. Les modules `voice/__tests__/` (205 tests), `customers/__tests__/customer.service.test.ts` (12 tests) et `telnyx.pipeline.test.ts` (17 tests) sont verts. Le TODO du vault était obsolète.
- [x] **Gift-cards Stripe réel — déployé en production** : code finalisé + déployé sur `feat/floor-plan-construction` (commit `da94d5fd`). API health 200, endpoint `/webhooks/stripe` joignable (400 sans signature attendu). Clés live et publishable key déjà présentes sur le VPS. Webhook Stripe Dashboard à vérifier : `https://api.sokar.tech/webhooks/stripe` avec `payment_intent.succeeded` et `payment_intent.payment_failed`.
- [ ] **Dette technique — env de test tsserver locké** : 2 tsserver.js tournent en background et bouffent 100% CPU à chaque typecheck. `find ... -name "*.tsbuildinfo" -delete && rm -rf .next .tsbuildinfo` corrige temporairement. À killer au reboot ou à disable dans VSCode (extensions TS). **Non-bloquant**.
- [ ] **Dette technique — smoke test Sokar Connect skip** : `smoke.test.ts` est `describe.skip` car trop couplé aux signatures internes. Les tests unitaires (38/38) couvrent les 4 endpoints. Pour un vrai e2e, monter docker-compose + Playwright.

## Liens rapides

[[README]] [[Architecture]] [[Journal]] [[Telnyx Pipeline]] [[Sokar Connect P0]] [[API Endpoints]]
