# Contexte Sokar

> État courant, court et à jour. Historique complet : [[Journal]]. Log brut 2026 archivé : [[archive/Context-log-2026]].
>
> Tenue du fichier : les nouvelles entrées datées vont dans `Journal.md` (append), jamais ici. `Context.md` ne reçoit que les TODOs, décisions et l'activité récente, et doit rester sous ~30 Ko.

## TODOs actifs

- [ ] **Sokar Connect — Pilote fermé** : il ne reste que **10 restos réels** + **sitemap GSC**. La **résa via lien ChatGPT est validée le 2026-09-21** : `connectAgentic` activé sur `chez-sokar-demo`, JSON-LD `ReserveAction` exposé, deep link fonctionnel, réservation réelle créée avec `source=chatgpt` persistée, et `holdToConfirmRate` sorti de zéro (4 SLO `met: true`, health GREEN — mais **n=1**, donc sans valeur statistique). Le monitoring P1 (`GET /api/internal/connect-kpis`, page `/dashboard/connect/pilot`) est déployé depuis le 2026-07-27.
- [x] **Sécurité — KPIs internes sans authentification** : **résolu le 2026-09-21**. `/api/internal/connect-kpis` et `/api/internal/pilot-kpis` répondaient 200 depuis Internet. Audit de la famille complète : les 11 autres routes `/api/internal/*` étaient déjà couvertes (les routes mutantes par `requireSokarOperator()`, `usage/margin` et `marketing/reconciliation` par token, en 503 quand le secret manque). Les deux fuites se limitaient à de la lecture agrégée. Garde `requireSokarOperator()` ajoutée sur les deux routes — le dashboard les appelle depuis le navigateur à travers le proxy Clerk, donc l'opérateur reste autorisé via `SOKAR_OPERATOR_USER_IDS`, déjà provisionné en prod. Aucun nouveau secret.
- [x] **Sokar Connect — fiches fictives publiées en prod** : **résolu le 2026-09-21**. Les 9 fiches `chez-sokar-*` (publiées le 2026-06-28 23:07 par un `db:seed` lancé sur la base prod sans `NODE_ENV`) ont été dépubliées en production via `pnpm --filter @sokar/database unpublish:demo-restaurants -- --apply`. Sitemap `sokar.tech` : 14 → 3 URLs ; `/restaurant/chez-sokar-veggie-paris` → 404 ; `/restaurants/paris` → 404 ; `/restaurants/lyon` → `noindex` (`totalInCity: 1`, `reason: not_enough_inventory`) ; `chez-sokar-demo` conservée (200 + JSON-LD). Conséquence à garder en tête : la vitrine publique ne contient plus que la fiche démo, donc le pilote a besoin des 10 restos réels pour que la surface ait du sens.
- [x] **Dette technique — Voice tests cassés (17 failures pré-existantes)** : résolu — `vitest run` API : 120 test files pass, 1235 tests pass, 6 skipped. Les modules `voice/__tests__/` (205 tests), `customers/__tests__/customer.service.test.ts` (12 tests) et `telnyx.pipeline.test.ts` (17 tests) sont verts. Le TODO du vault était obsolète.
- [x] **Gift-cards Stripe réel — déployé en production** : code finalisé + déployé sur `feat/floor-plan-construction` (commit `da94d5fd`). API health 200, endpoint `/webhooks/stripe` joignable (400 sans signature attendu). Clés live et publishable key déjà présentes sur le VPS. Webhook Stripe Dashboard à vérifier : `https://api.sokar.tech/webhooks/stripe` avec `payment_intent.succeeded` et `payment_intent.payment_failed`.
- [ ] **Dette technique — env de test tsserver locké** : 2 tsserver.js tournent en background et bouffent 100% CPU à chaque typecheck. `find ... -name "*.tsbuildinfo" -delete && rm -rf .next .tsbuildinfo` corrige temporairement. À killer au reboot ou à disable dans VSCode (extensions TS). **Non-bloquant**.
- [ ] **Dette technique — smoke test Sokar Connect skip** : `smoke.test.ts` est `describe.skip` car trop couplé aux signatures internes. Les tests unitaires (38/38) couvrent les 4 endpoints. Pour un vrai e2e, monter docker-compose + Playwright.

## Décisions récentes

2026-09-21 — [connect, database, seed, sécurité] **Le seed ne publie plus de fiches fictives sur une base distante** — Le garde passe de `NODE_ENV !== 'production'` (qui échouait en mode ouvert) à un raisonnement sur l'hôte de `DATABASE_URL`. Les fiches de démo `chez-sokar-*` ne se créent plus que sur `localhost`/`127.0.0.1`/`::1`, ou sur une base distante avec l'opt-in explicite `SEED_DEMO_RESTAURANTS=true`. Conséquence opérationnelle : si le seed doit alimenter les pages locales de staging, il faut désormais poser cet opt-in — `env.md` le documente. La fiche `chez-sokar-demo` reste hors garde et se crée partout.

2026-09-21 — [codex, performance, plugins, config] **Configuration Codex allégée** — Les entrées résiduelles Google Calendar et Slack sont désactivées (elles étaient déjà absentes côté gestionnaire de plugins). L’ancien plugin `computer-use` est désactivé ; `unified-computer-use`/`cua_repl` et le pont Chromium utilisé avec Brave restent actifs, GitHub est conservé. Le raisonnement par défaut passe de `max` à `high` pour réduire la latence ; `max` reste disponible comme choix ponctuel. Mesure sur session fraîche à confirmer.

- **2026-07-25** — Durcissement voice : 8 lots mergés sur `main` (`a805c65`). Validation Zod source unique pour les 8 outils, annulation ambiguë avec cascade de résolution (téléphone → nom sûr → temps → transfert gérant), création replay-safe via `callId`, reconstruction des tool calls du stream (élimine le double LLM call), timeout 8s + circuit breaker (3 échecs → 30s skip) + fallback réseau, fallback mid-stream avec distinction session abort vs timeout, harness de régression déterministe (10 scénarios E2E), audit PII global (Sentry extra nettoyés, `redactPii()` sur debug logs). 1509 tests, 0 régression. Non déployé.
- **2026-07-24** — Voice LLM : Gemma 4 31B sur Cerebras devient le primaire (intelligence privilégiée sur la latence, TTFT ~403 ms), Llama 3.3 70B sur Groq via OpenRouter en fallback automatique (429/5xx, TTFT ~151 ms). Architecture bidirectionnelle via `VOICE_LLM_PROVIDER` (« cerebras » défaut, « openrouter » alternative). Remplace Gemini 3.5 Flash-Lite (google-vertex, ~696 ms). Benchmarks éliminés : Qwen3.6-27B (0/6 tool calls), GPT-OSS-120B (6/6 mais 563 ms avg). Commit `8be510b` sur `migration/frankfurt`.
- **2026-07-21** — Floor-plan : la liste d’attente est une vue Live service dédiée et ne permet pas de forcer une table depuis le client. La promotion utilise exclusivement l’allocation transactionnelle existante côté API ; le drag-and-drop vers une table est différé.
- **2026-07-21** — Floor-plan : l’allocation expose d’abord trois propositions explicables et read-only ; undo/redo reste volontairement borné à la géométrie réversible du canvas (50 actions locales, pas de création/suppression). La machine à états de service complète reste différée aux retours de pilotes.
- **2026-06-24** — Sokar Connect v1.1 validée par Hamza (8 corrections v1→v1.1 intégrées : static export→standalone, suppression basePath, VPS+Caddy+Cloudflare proxy, gating corrigé, source de vérité unique, seed local/staging, audit log métier only, CORS durci). Spec : [[Sokar Connect P0]]
- **2026-06-24** — Phase 0 Sokar Connect lancée : ordre T1→T2+T3→T4 avec STOP revue entre chaque. T1 (`migrate deploy`) **fait le 2026-06-24** (backfill Chez Sokar → Lyon/69001/FR), T2-T10 (API publique, JSON-LD, app connect, pages, analytics, sécurité) écrits et testés (38/38 tests verts). Reste : T4 app en prod + P1 pilote fermé (cf. [[Sokar Connect P0]]).
- **2026-06-24** — Cleanup docs/ : `pilot/_archive/` créé, `runbook.md` extrait de `pilot/`, 3 versions agentic obsolètes archivées. Vault Obsidian re-activé après 5 semaines d'arrêt (cf. Journal).
- **2026-06-23** — Cleanup Vault Obsidian : 17 notes. Pas alimenté depuis 2026-05-21. Rétro-documentation des 5 semaines sautées : agentic-reservations P0, MCP OAuth, OpenAI Reserve, model switch Hermes, VSCode crashes. Reprise automatic à partir de T1 Sokar Connect.
- **2026-06-23** — Model switch Hermes : `glm-5.2` → `minimax-m3` via `opencode-go`. Supervisor `glm-5.2=review`. Crof.ai supprimé (404 sur `deepseek-v4-pro`). Contrainte : `/v1/messages` exige `x-api-key` (Hermes n'envoie pas) → `api_mode: anthropic_messages` désactivé.
- **2026-06-22** — RGPD Phase 5 durci : three-token pattern (OTP → verification token → one-shot action) sur `/api/rgpd/erase` et `/api/rgpd/export`. Modèles `IdentityVerificationOtp` + `SignedTokenUsage`. Phase 7 a ajouté identity hardening.
- **2026-06-21** — Agentic-reservations P0 livré : hold/quote state machine, partial unique index `one_active_hold_per_slot`, idempotency scoped, audit log append-only, RGPD structured consents. Spec v3.2 = référence prod-safe.
- **2026-06-21** — Migration TTS ElevenLabs → Cartesia Sonic 3.5 finalisée (cf. [[Telnyx Pipeline]]). Limitation speed/volume sur sonic-3.5 (depuis avril 2026), workaround = revenir à sonic-3 si besoin.
- **2026-05-21** — Crof AI `deepseek-v4-pro` ne fonctionne pas comme provider `custom` (404). Le provider normalise le model name en `deepseek/deepseek-v4-pro` que Crof rejette. **Résolu** par switch vers `opencode-go` + `minimax-m3` (cf. 2026-06-23).
- **2026-05-21** — Clerk mocké en test via `helpers.ts` (`requireOrg` lit `Authorization` header). Telnyx webhook guard mocké dans `reservation.test.ts`.

2026-09-21 — [dashboard, floor-plan, live, mobile, iphone13, ux, ui] **Tablist Live désimbriquée** — La barre `Plan / Liste d’attente / Statistiques` ne dessine plus un conteneur bordé à l’intérieur du header déjà bordé : suppression de la bordure, du fond et du padding de la `tablist`, conservation d’un seul pill pour l’onglet actif. Contrôle navigateur Live 390×844 : hiérarchie visuelle aplatie, date et filtre distincts dessous ; FloorPlanCanvas 53/53, typecheck et Prettier verts.

2026-09-21 — [dashboard, floor-plan, live, mobile, iphone13, ux] **Navigation Live remplace le bandeau de pouls** — Suppression du bandeau `Sous contrôle / Aucune réservation` dans le cockpit Live. Les onglets `Plan`, `Liste d’attente` et `Statistiques` sont désormais le premier repère visuel, dans une barre pleine largeur ; la date et le filtre serveur restent sur la ligne suivante. Le calcul compact conserve seulement le masquage des KPI à zéro et le cadrage du plan, sans message de statut redondant. Contrôle navigateur : Live 390×844 sans bandeau de pouls, tablist accessible en premier ; FloorPlanCanvas 53/53, typecheck et Prettier verts.

2026-09-21 — [dashboard, floor-plan, edit, mobile, iphone13, ux, ui] **Toolbar tactile harmonisée** — Dernière passe visuelle de la toolbar d’édition : les groupes Aides au placement et Historique reçoivent une ombre discrète, leurs hauteurs restent à 46 px et le bouton `Enregistrer` mobile passe à 44 px avec coins `rounded-xl`, pour aligner visuellement l’action principale et respecter une cible tactile confortable. Contrôle navigateur 390×844 : aucun rognage ni débordement, alignement stable.

2026-09-21 — [dashboard, floor-plan, edit, mobile, iphone13, ux] **Libellé de sauvegarde complet** — Le bouton mobile n’abrège plus l’action en `Sauver` : il affiche maintenant `Enregistrer` avec une largeur minimale de 6,5 rem, tout en restant dans la toolbar 390 px sans rognage. Le libellé desktop et l’ARIA restent cohérents. Validation navigateur après rechargement : bouton complet visible ; FloorPlanCanvas 53/53, typecheck et Prettier verts.

2026-09-21 — [dashboard, floor-plan, edit, mobile, iphone13, ux] **Toolbar mobile d’édition rééquilibrée** — Dans le canvas `edit-plan`, la ligne grille/magnétisme · historique · sauvegarde utilise désormais `justify-between` sous `sm` au lieu de laisser un grand vide avant `Sauver`. Les groupes sont alignés sur 46 px, les actions Annuler/Rétablir gardent des cibles tactiles carrées et le bouton de sauvegarde conserve une largeur minimale stable ; à partir de `sm`, l’alignement desktop à droite reste inchangé. Contrôle navigateur 390×844 : aucun débordement, trois blocs lisibles et espacés. Vitest FloorPlanCanvas 53/53, typecheck et Prettier verts.

2026-09-21 — [dashboard, floor-plan, edit, mobile, ux] **Sélecteur « Salle » retiré de la toolbar** — Le sélecteur de nom de plan dans la barre du canvas d’édition faisait doublon avec la navigation globale et prenait de la place sur iPhone 13. Il est supprimé de la vue mobile et les contrôles de grille/magnétisme commencent désormais directement la toolbar ; les paramètres de dimensions restent accessibles via une icône compacte sur desktop. Validation navigateur : plus aucun bouton `Salle` dans le canvas, contrôles et bouton `Sauver` lisibles. Vitest FloorPlanCanvas 53/53, typecheck, Prettier et diff-check verts.

2026-09-21 — [dashboard, floor-plan, edit, ux] **En-tête Édition simplifié** — Dans la vue `edit-plan`, suppression du fil d’Ariane `Plans / Salle` afin de libérer l’espace visuel. Les onglets `Plan visuel` / `Sections & tables` et l’action `+ Nouveau plan` sont maintenant regroupés sur une seule ligne, avec un conteneur sans retour à la ligne et des contrôles résistants au rétrécissement mobile. Validation navigateur sur l’écran Édition, Vitest page 12/12, typecheck dashboard et Prettier verts.

2026-09-21 — [dashboard, nextjs, dev, ops] **Cache Next.js local réparé** — Le serveur 3002 rencontrait un bundle serveur incrémental incohérent (`__webpack_modules__[moduleId] is not a function`) sur le proxy `/api/proxy/[...path]`, ce qui produisait un écran 500 dans Brave. Le processus de développement a été arrêté, seul le cache généré `apps/dashboard/.next` a été supprimé, puis `next dev -p 3002` a été relancé. Vérification navigateur : `/dashboard/floor-plan?view=service-live` répond 200, les appels proxy répondent 200 et l’écran Live affiche ses trois onglets. Aucun code métier ni schéma modifié.

2026-09-21 — [dashboard, floor-plan, live, mobile, iphone13, ux, accessibility] **Audit Live iPhone 13 appliqué** — Après une seconde lecture en viewport 390×664, la navigation du service sépare les onglets sur une ligne complète puis la date et le filtre sur une seconde ligne : `Statistiques` reste ainsi réellement cliquable et le filtre ne rogne plus « Toutes les tables ». La date conserve son input natif accessible mais affiche un libellé français superposé (`21 sept. 2026`) ; l'indicateur WebKit est masqué uniquement sous 640 px. Les cartes compactes affichent désormais un état court (`Libre`, `Réservée`, `Arrivée`, etc.) tout en conservant le libellé complet pour l'accessibilité. Le bandeau vide est raccourci sur mobile et la surface Live reçoit une hauteur minimale adaptée au viewport pour supprimer le vide mort sous la légende. Validation : contrôle navigateur iPhone 13 (onglets sans chevauchement, filtre icône, date française, statistiques cliquables, cartes `Libre`), Vitest FloorPlanCanvas 53/53, typecheck dashboard et diff-check verts. E2E Playwright non relançable dans le sandbox local (Chromium headless bloqué par `bootstrap_check_in`/`EPERM`) ; aucun commit, push, staging ou production.

2026-09-20 — [dashboard, floor-plan, editor, mobile, ipad, iphone13, ux] **Éditeur de plan : géométrie fidèle et édition tactile rapide** — Le canvas représente enfin une table `round` par un cercle et des assises radiales. L’inspecteur desktop et la fiche tactile permettent de renommer une table et d’ajuster ses couverts sans quitter le plan ; un nom personnalisé (`T1 bis`, `Banquette 1`) est désormais aussi celui affiché sur le canvas. Le drag affiche des guides d’alignement sur bords/centres, et la duplication cherche une position libre au lieu de recouvrir la source. Sous 1024 px, la palette et l’inspecteur latéral cèdent la place à une fiche flottante : elle est rendue par portal hors du conteneur à overflow, au-dessus de la navigation, avec une largeur adaptée à l’iPad. La toolbar est compactée pour le viewport réel iPhone 13. Tests ciblés : Vitest 53/53, typecheck dashboard, E2E iPhone 13 (2 passants), iPad Mini (4 passants) et bureau 1440 (3 passants) ; scénarios hors viewport explicitement ignorés. Aucun commit, push, staging ou production.

2026-09-20 — [dashboard, floor-plan, sevenrooms, zenchef, mobile, live, ux] **Ergonomie FOH & plan de salle inspirée de SevenRooms & Zenchef** — Implémentation des techniques de référence issues de SevenRooms et Zenchef sur le plan de salle (`FloorPlanCanvas.tsx`). (1) Quick-Seat Walk-in 1-tap (SevenRooms) : attribution directe d’une table libre avec dimensionnement dynamique selon sa capacité (`⚡ Installer (4 pers.)` + chip `2 pers.` quand capacité > 2), ouvrant instantanément la table sans formulaire intermédiaire lourd. (2) Minuteur de service écoulé (SevenRooms) : affichage en direct du temps passé à table (`À table depuis X min` avec icône Clock3) pour toute table occupée dans le bottom sheet et l’inspecteur desktop. (3) Duplication instantanée 1-tap (SevenRooms / Toast) : bouton `Dupliquer` clonant immédiatement la table sélectionnée (+32 px de décalage géométrique, nom auto-incrémenté T1→T2→... et sélection directe de la copie), disponible dans l’inspecteur desktop et dans la fiche tactile mobile. (4) Bouton de rotation 1-tap `Pivoter 90°` : rotation d’un toucher éliminant la manipulation minutieuse des poignées 12 px sur écran tactile. (5) Bottom sheet mobile d’édition tactile : ouverture d'une fiche basse élégante flottante au-dessus de la navigation (`floor-plan-mobile-service-sheet md:hidden`) dès qu'une table ou un groupe de tables est sélectionné, intégrant duplication, rotation 90°, bascule forme rect/ronde, sélecteur de section, retrait du plan et combinaison/déliaison (Zenchef). Suite de tests Vitest 39 fichiers / 254 tests 100 % passants, typecheck TypeScript strict, stylelint CSS et validation visuelle Playwright sur iPhone 13.

2026-09-20 — [dashboard, floor-plan, edit, mobile, iphone13, ui, ux] **Édition du plan sur mobile : toolbar resserrée, tiroir palette et inspecteur tactile** — Refonte ergonomique complète de la rubrique Édition (`/dashboard/floor-plan?view=edit-plan`) sur iPhone 13. (1) Toolbar mobile resserrée sans débordement horizontal : boutons zoom masqués sur mobile (`hidden sm:flex`, pinch tactile prenant le relais), commandes de grille/aimant et annuler/rétablir compactes, sélecteur de plan calibré et bouton de sauvegarde systématiquement visible avec libellé adapté (« Sauver » sur mobile, « Enregistrer » sur sm+). (2) Palette d'aménagement mobile tactile : masquage du bandeau horizontal sous le canevas (`hidden md:flex`), intégration d'une barre d'action basse flottante (`fixed inset-x-3 bottom-[...] z-40`) avec boutons « Tables à placer (N) » et « + Ajouter », ouvrant un tiroir/dialogue plein écran ergonomique pour placer instantanément tables, zones, murs, portes et bar ou lancer la disposition automatique. (3) Fiche d'action tactile (inspecteur mobile) : au toucher d'une table, mur ou zone en mode édition, ouverture d'un bottom sheet flottant (`floor-plan-mobile-service-sheet z-50`) offrant les contrôles directs (formes rect/ronde, rotation +90° par appui, sélecteur de section, retrait du plan, suppression) sans manipuler de petites poignées 12 px. (4) Suppression du doublon de capacité : masquage du badge haut en mode édition, laissant un seul indicateur « 4 places ». (5) Hauteur du canevas plein écran mobile (`h-[calc(100dvh-13.5rem)]`) supprimant les défilements parasites. Validation visuelle iPhone 13 en thèmes clair et sombre (Playwright) ; suite Vitest 39/254, typecheck TypeScript et stylelint CSS 100 % verts.

2026-09-20 — [dashboard, floor-plan, live, mobile, iphone13, ui, ux] **Harmonisation visuelle Live : statuts explicites, sélecteur de date français et cadrage ergonomique** — Traitement des imperfections résiduelles signalées sur iPhone 13. (1) Statut des tables cohérent et lisible : remplacement du rail gris terne par le vert émeraude vif (`bg-emerald-500`) sur tables libres, affichage systématique de la pastille émeraude et du statut en toutes lettres (`● Libre`, `Occupée` ou nom client) en synergie directe avec le compteur bas (`● 3 disp.`). (2) Correction du sélecteur de date : remplacement de l'input natif brut tronqué (`09/20/2026 [`) par un libellé français net avec icône calendrier (`[ 📅 20 sept. 2026 ]`) et superposition transparente de l'input pour déclencher le picker natif au toucher sans altérer l'accessibilité ni les tests (`aria-label="Date du service"`). (3) Ergonomie tactile et occupation de l'espace : augmentation des dimensions minimales des cartes compactes (`112×60 px`) avec badge de capacité dédié (`4 pl.`), et centrage vertical équilibré (`my-auto`) éliminant le vide inférieur sans risque de tronquage horizontal sur T3. Validation visuelle iPhone 13 en thèmes clair et sombre (Playwright) ; suite Vitest 39 fichiers / 254 tests, typecheck et stylelint CSS 100 % verts.

2026-09-20 — [dashboard, floor-plan, live, mobile, iphone13, ui, ux] **Cadrage intérieur, contraste & ergonomie Live mobile optimisés** — Élimination du vide mort supérieur (~160 px) : remplacement de `m-auto` par `mx-auto mt-2 mb-auto sm:my-auto` sur `floor-plan-live-stage`, ancrant les tables sous la barre d'outils avec marge naturelle. Suppression de l'artefact de découpe de zone : `ZoneCard` passe en `border-0 bg-transparent` en mode Live, évitant les bordures tronquées sur T3 et la démarcation grise au milieu du canevas blanc. Contraste renforcé des tables : ombre portée `shadow-md`, liseré `border-floor-table-muted/25`, typographie du nom `text-xs font-bold` et pastille de statut `size-2`. Lisibilité mobile rétablie : affichage systématique de la capacité (`● 4 pl.`) ou du nom client en mode compact au lieu d'une carte anonyme. Réactivation du pinch-to-zoom à deux doigts et du double-tap de recadrage en mode Live pour offrir une manipulation fluide de la salle au restaurateur. Validation visuelle iPhone 13 en thème clair et sombre ; suite Vitest 39/254, typecheck et stylelint CSS 100 % verts.

2026-09-20 — [dashboard, floor-plan, live, mobile, iphone13, ux, ui] **Validation iPhone 13 & optimisation plein écran Salle Live** — Test d'accès direct émulé iPhone 13 (390×844) sur localhost. Élimination complète de l'espace mort inférieur : ajout de la classe `.dashboard-shell-content--salle` avec `padding-bottom: calc(4.75rem + ...)` et flex-grow sur `main` et la carte du plan, supprimant les 120 px de vide résiduel. Remplacement du calcul rigide par un agencement flex complet (`flex-1 min-h-0 w-full`) dans `CardContent`, le conteneur DnD et `canvasViewportRef`. Le bouton filtre serveur sur la barre d'outils mobile adopte une icône carrée 32×32 px avec masquage forcé du texte (`[&>span]:!hidden`) et du chevron, évitant le rognage « T. » sur écran 390 px. Raccordement de `mobileLegendDialog` dans le rendu principal pour ouvrir la modale « Statuts des tables » au clic sur « Légende ». Intégration d'un `ResizeObserver` avec debounce 100 ms pour recadrer automatiquement la salle lors des changements d'orientation ou de dimension. Validation visuelle par captures d'écran Playwright (`iphone13_live_initial`, `iphone13_table_selected`, `iphone13_legend_open`, `iphone13_walkin_open`) ; suite dashboard 39/254, typecheck et stylelint CSS 100 % verts.

2026-09-20 — [dashboard, floor-plan, live, mobile, ux] **Cadrage Live resserré** — Le cadre opérationnel se cale maintenant sur les tables placées avec une marge Live courte de 24 px au lieu de reprendre la hauteur complète de la zone. L’auto-fit peut dépasser 100 % jusqu’au plafond normal de 200 % pour agrandir les cartes sans réintroduire de vide sous le groupe. La zone continue d’être peinte mais est écrêtée par la scène ; son nom est reporté dans un badge fixe en haut du cadre pour rester lisible. Suite ciblée 50/50, typecheck Dashboard, lint CSS, Prettier, diff-check et contrôle mobile à 591 px verts ; aucun commit, push, staging ou production.

2026-09-20 — [dashboard, floor-plan, live, mobile, ux] **Live finalisé sur la zone utile** — Le mode Live est maintenant cadré sur les zones qui contiennent au moins une table placée et sur les tables effectivement créées, avec une marge fonctionnelle ; les zones vides, murs et espace de salle inutilisé ne polluent plus la vue. Le zoom, le dézoom, le pinch, le double-tap et les commandes de viewport sont retirés de Live : l’éditeur conserve le plan complet et ses outils. La hauteur du plan Live suit son contenu, sans grand vide sous la zone ; l’overflow reste borné à cette surface utile. Suite ciblée 50/50, typecheck Dashboard, lint CSS, Prettier, diff-check et contrôle navigateur à 591 px verts ; aucun commit, push, staging ou production.

2026-09-20 — [dashboard, floor-plan, live, mobile, ux] **Cadrage utile du plan fiabilisé** — Le canvas zoomé recouvre explicitement son espaceur de scroll depuis l’origine de la fenêtre. Chaque plan reçoit aussi un cadrage d’ouverture propre sur ses tables : un zoom ou un défilement conservé depuis une autre salle ou un rafraîchissement ne peut plus laisser les tables hors champ. En Live mobile, elles apparaissent immédiatement sous les commandes avec une marge confortable ; après ce premier cadrage, zoom et déplacement manuels restent respectés. Tests ciblés 49/49, typecheck Dashboard, lint CSS, Prettier, diff-check et contrôle navigateur à 591 px verts ; aucun commit, push, staging ou production.

2026-09-19 — [dashboard, floor-plan, live, editor, ui] **Carte de table unifiée inspirée de la référence** — Les tables de Live et de l’éditeur utilisent maintenant la même carte compacte : corps ardoise horizontal d’au moins 132 × 66 px en sombre, blanc en clair avec assises blanches et liseré gris, ratio visuel proche de 2:1 quand la table s’élargit, quatre demi-assises positionnées sur les bords, numéro en haut à gauche, capacité ou client/statut en bas, et rail coloré étroit à droite (`Occupée`, `Réservée`, ou accent visuel en édition). La sélection de l’éditeur reprend ces tokens avec un liseré interne et des poignées cyan discrètes, sans cadre autour de la géométrie ; les poignées sont absentes en Live, où la sélection s’affiche aussi par un liseré interne. Sur mobile, une table Live sélectionnée ouvre un bottom sheet compact au-dessus de la navigation avec son statut et les actions métier prioritaires. Le nom de table et le statut ont une hiérarchie renforcée, avec un point de statut coloré en Live. Le glisser-déposer des réservations reste actif. Les couleurs sont des tokens dédiés `floor-table-*`, avec tests de rendu éditeur et de table occupée. Suite dashboard 39/242, typecheck, lint CSS, Prettier et diff-check verts ; aucun commit, push, staging ou production.

2026-09-19 — [dashboard, floor-plan, editor, ux] **Disposition initiale automatique** — Quand un plan d'édition contient déjà des tables métier mais qu'aucune n'est encore positionnée, le premier chargement les répartit automatiquement sur la grille. L'action est limitée au premier chargement d'un plan entièrement non placé, ne s'exécute pas après une création manuelle ni en mode Live, et conserve une entrée Annuler/Rétablir ainsi qu'un message discret indiquant que l'enregistrement reste à faire. Les tables déjà positionnées restent inchangées. Suite dashboard 39 fichiers / 242 tests, typecheck, Prettier et diff-check verts ; aucun commit, push, staging ou production.

2026-09-19 — [dashboard, floor-plan, editor, spatial, schema, api] **Zones persistantes et tables combinables** — Le plan visuel conserve désormais les zones nommées, avec rattachement optionnel à une section, déplacement et redimensionnement direct par poignée ; ces deux gestes entrent aussi dans l’historique Annuler/Rétablir. Les tables sélectionnées peuvent être marquées comme combinables sans dupliquer les tables métier ; la relation est persistée et signalée discrètement sur le canvas. La palette garde `Zone` sous `AMÉNAGEMENT`, l’état vide propose `Placer les tables` et `Disposition automatique`, et les tables existantes restent la source unique. La migration additive `20260919010000_floor_plan_spatial_structure` et les routes CRUD sont présentes ; le SQL a été appliqué localement à `sokar` et `sokar_preview` sans donnée de test résiduelle. Dashboard 39/241 et tests floor-plan API 63/63 verts, typechecks Prisma/API/dashboard, lint CSS, Prettier, diff-check et contrôle navigateur verts ; la suite API globale conserve 12 échecs préexistants liés au WebSocket voice et à la restriction `listen EPERM` du sandbox. Aucun commit, push, staging ou production.

2026-09-19 — [dashboard, floor-plan, editor, ux, responsive] **Tables métier comme source du plan** — `Plan visuel` ne crée plus de formes de tables génériques dans sa palette : il expose les tables existantes sans position (`Tables à placer · N`), permet de les déposer ou de les placer par clic, propose une disposition automatique et offre une création en lot pour démarrer rapidement. Les tables placées restent visibles sans chaises décoratives ; capacité, nom et section restent des données métier, tandis que forme/taille sont une représentation visuelle. L’inspecteur d’une table est réduit à la forme, la section et `Retirer du plan`, et les paramètres de salle vivent dans le menu `Salle`. Suite dashboard 39 fichiers / 241 tests, typecheck, lint CSS, Prettier, diff-check et contrôle navigateur verts ; branche dev locale uniquement.

2026-09-19 — [dashboard, floor-plan, editor, ux, responsive] **Dimensions physiques du plan** — La modale `Paramètres de la salle` expose maintenant `Largeur` et `Longueur` en mètres, accepte la virgule ou le point décimal et valide une plage de 2 à 100 m. Le moteur conserve les pixels à 100 px/m ; la légende du canvas affiche les mètres et `1 carreau = 16 cm`. Le bouton devient `Appliquer`, la copie est compacte et les portails Dialog héritent du thème courant pour éviter une modale noire en mode clair. Suite dashboard 39 fichiers / 241 tests, typecheck, lint CSS, Prettier, diff-check et contrôle navigateur verts ; branche dev locale uniquement.

2026-09-19 — [dashboard, floor-plan, editor, ux, responsive] **Palette rééquilibrée** — La palette desktop passe à 144 px pour garder `Table ronde` et `Table rectangulaire` sur une ligne. Les cinq cartes partagent la même largeur et la même hauteur minimale ; les groupes `TABLES` et `ÉLÉMENTS` rendent la hiérarchie explicite. Les toggles conservent leur point d’état mais reposent d’abord sur un fond actif, un liseré discret et un contraste de texte. Suite dashboard 39 fichiers / 241 tests, typecheck, lint CSS, Prettier et diff-check verts ; branche dev locale uniquement.

2026-09-19 — [dashboard, floor-plan, editor, ux, responsive] **Toolbar et palette affinées** — `Grille` et `Magnétisme` signalent leur état avec une surface active, un liseré discret et un point d’état ; `Paramètres` est isolé du groupe Annuler/Rétablir et se marque lorsqu’il est ouvert. La palette desktop passe à 96 px, conserve une hauteur exploitable et affiche `Table ronde` / `Table rectangulaire` sans ambiguïté. Le canvas et l’état vide restent inchangés. Suite dashboard 39 fichiers / 241 tests, typecheck, lint CSS, Prettier et diff-check verts ; branche dev locale uniquement.

## Liens rapides

[[README]] [[Architecture]] [[Journal]] [[Telnyx Pipeline]] [[Sokar Connect P0]] [[API Endpoints]]
