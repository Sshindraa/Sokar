# Hermes Agent — Configuration Sokar
# Architecture: OpenCode Go primary → OpenRouter fallback
# Copier vers ~/.hermes/config.yaml pour appliquer

model:
  provider: custom
  default: deepseek-v4-flash
  base_url: https://opencode.ai/zen/go/v1
  key_env: OPENCODE_GO_API_KEY
  fallback:
    provider: openrouter
    model: deepseek/deepseek-v4-flash
    base_url: https://openrouter.ai/api/v1
    api_key: ''
  persistent: true

skills_dir: /Users/hamza/Desktop/Sokar/agent/skills

# Cascade (kimi-k2.6) planifie → Hermes (deepseek-v4-flash) exécute via MCP
